interface BBox {
    x: number;
    y: number;
    width: number;
    height: number;
}

interface Coordinate {
    x: number;
    y:number;
}


interface WaypointData{
    connection: number,
    point: Coordinate,
}